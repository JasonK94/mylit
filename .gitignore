# .gitignore에서는 line 중간에 있는 주석을 주석으로 생각하지 않는다. 주의할것.
# 이미 추적 중인 걸 무시하려면, git rm (-r = recursive) --cached(파일이 아니라 git 추적을 삭제한다는 의미) (folder/file)를 하면 된다. 

# 파일변경 + 스테이징 + 커밋 취소: git reset --hard HEAD~ (HEAD~=HEAD~1; 마지막 1개 커밋까지. HEAD~2: 마지막 두개 커밋.)
# 스테이징 + 커밋 취소: git reset HEAD~ (=git reset --mixed HEAD~)
# 커밋 취소: git reset --soft HEAD~




# R 패키지 빌드 아티팩트
# R CMD check 출력
myR.Rcheck/   
# devtools::build() 결과물
myR_*.tar.gz

# RStudio
.Rhistory
.RData
.Rproj.user/

# Python
__pycache__/
*.pyc

# 기타
ipynb/
ipynb/output/
ipynb/temporary/

# new rmd files; .../로 하면 그 하위파일을 재귀적으로 무시하는데, .../*로 하면, 직속 파일만 무시한다.
rmd/myR/inst/extdata/NicheNetData
rmd/

# Environment variables
.env

# data, logs, outputs...
*output*
*plots*
projects/
trash/
renv/
*renv*
*bin
*deprecated*

*.rds
*.log
*.pdf
*.png
*.csv
*/data/*
*.tar.gz
*.gz
*.qs
*sobj*
*h5ad
*h5seurat
*loom
*mtx
*mtx.gz

# Internal documentation (not for public repository)
docs-main/SECURITY_REVIEW_REPORT.md
docs-main/GIT_PUBLIC_REPOSITORY_PLAN.md

