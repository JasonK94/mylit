% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_volcano.R, R/test_claude.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano Plot}
\usage{
plot_volcano(
  lmm_summary,
  term_pattern = NULL,
  effect_threshold = 0.5,
  p_threshold = 0.05,
  label_top = 10,
  title = "Volcano Plot"
)

plot_volcano(
  lmm_summary,
  term_pattern = NULL,
  effect_threshold = 0.5,
  p_threshold = 0.05,
  label_top = 10,
  title = "Volcano Plot"
)
}
\arguments{
\item{lmm_summary}{Data frame from run_lmm_analysis()$summary.}

\item{term_pattern}{String. Regex to filter model terms.}

\item{effect_threshold}{Numeric. Threshold for "large effect".}

\item{p_threshold}{Numeric. P-value significance threshold.}

\item{label_top}{Integer. Number of top genes to label.}

\item{title}{String. Plot title.}

\item{data}{data.frame with statistical results (i.e. FindMarkers object, LMM summary object, etc.)}

\item{x_col}{Character. Column name for effect size/estimate (default: "estimate")}

\item{y_col}{Character. Column name for p-value (default: "p_value")}

\item{gene_col}{Character. Column name for gene/feature names (default: "gene")}

\item{filter_col}{Character. Column name to filter by pattern (default: "term")}

\item{filter_pattern}{Character. Regex pattern to filter rows}

\item{p_adj_col}{Character. Column name for adjusted p-value (optional)}

\item{use_p_adj}{Logical. Use adjusted p-value if available (default: TRUE)}

\item{label_col}{Character. Column name for labels (default: gene_col)}

\item{resize_x}{Logical. Auto-resize x-axis limits (default: TRUE)}

\item{resize_y}{Logical. Auto-resize y-axis limits (default: FALSE)}

\item{xlab}{Character. X-axis label (default: "Effect Size")}

\item{ylab}{Character. Y-axis label (default: "-log10(p-value)")}

\item{colors}{Named character vector. Custom colors for categories}

\item{point_size}{Numeric. Point size (default: 2)}

\item{point_alpha}{Numeric. Point transparency (default: 0.6)}
}
\value{
ggplot object

ggplot object.
}
\description{
Creates a volcano plot from statistical results (e.g., differential expression,
linear model results). Supports flexible column naming and filtering.

Visualizes effect sizes and p-values for a specific model term.
}
\examples{
\dontrun{
# Basic volcano plot
p <- plot_volcano(lmm_results, x_col = "estimate", y_col = "p_value")

# With filtering
p <- plot_volcano(lmm_results, filter_pattern = "drug.*timepoint", label_top = 20)

# Using adjusted p-values
p <- plot_volcano(lmm_results, p_adj_col = "p_adj", use_p_adj = TRUE)
}

plot_volcano(
  lmm_summary = lmm_res$summary,
  term_pattern = "drug.*:.*timepoint",
  title = "Drug-by-Time Interaction Effects"
)
}
