% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_analysis.R
\name{myGO}
\alias{myGO}
\title{Comprehensive GO and GSEA Analysis}
\usage{
myGO(
  DEG = NULL,
  seurat_obj = NULL,
  pathway_set = NULL,
  pathway = NULL,
  analysis_type = c("GO", "KEGG", "GSEA", "ALL"),
  go_ontology = c("BP", "MF", "CC"),
  fc_threshold = 0.25,
  p_use = "p_val_adj",
  pval_threshold = 0.05,
  gsea_min_size = 15,
  gsea_max_size = 500,
  return_plots = FALSE
)
}
\arguments{
\item{DEG}{Data frame containing differential expression results with required columns:
\code{gene}, \code{avg_log2FC}, \code{p_val}, \code{p_val_adj}}

\item{seurat_obj}{Seurat object (currently not used, for future compatibility)}

\item{pathway_set}{Character vector specifying which pathway databases to use.
Options include: "H", "C2_KEGG", "C2_REACTOME", "C5_BP", "C5_MF", "C5_CC".
If NULL, uses default set.}

\item{pathway}{Character string to filter results for specific pathway names (optional)}

\item{analysis_type}{Character string specifying analysis type: "GO", "KEGG", "GSEA", or "ALL"}

\item{go_ontology}{Character vector specifying GO ontologies: "BP", "MF", "CC"}

\item{fc_threshold}{Numeric threshold for log2 fold change significance (default: 0.25)}

\item{p_use}{Character string specifying which p-value column to use: "p_val_adj" or "p_val" (default: "p_val_adj")}

\item{pval_threshold}{Numeric threshold for p-value significance (default: 0.05)}

\item{gsea_min_size}{Minimum gene set size for GSEA (default: 15)}

\item{gsea_max_size}{Maximum gene set size for GSEA (default: 500)}

\item{return_plots}{Logical indicating whether to return plots (default: FALSE, not implemented)}
}
\value{
Named list containing analysis results for each method. Each element is a
data frame with columns: pathway, description, p_value, adj_p_value,
effect_size, gene_count, genes
}
\description{
Main function to perform comprehensive pathway enrichment analysis on
differential expression results from scRNA-seq data.
}
\examples{
\dontrun{
# Basic usage
results <- myGO(DEG = deg_dataframe, analysis_type = "ALL")

# Use raw p-values instead of adjusted
results <- myGO(DEG = deg_dataframe, p_use = "p_val", analysis_type = "GSEA")

# Focus on specific pathways
stroke_results <- myGO(DEG = deg_dataframe, pathway = "INFLAMMATORY")

# Access specific results
hallmark_gsea <- results$GSEA_H
go_bp_up <- results$GO_BP_UP
}
}
