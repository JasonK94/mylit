% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_gpt.R
\name{set_parallel_profile_v2}
\alias{set_parallel_profile_v2}
\title{Set parallel and threading profile safely (RStudio / Terminal)}
\usage{
set_parallel_profile_v2(
  mode = c("auto", "rstudio", "terminal"),
  workers = max(1, parallel::detectCores() - 1),
  blas_threads = 1,
  use_biocparallel = FALSE
)
}
\arguments{
\item{mode}{One of c("auto","rstudio","terminal").}

\item{workers}{Number of workers for \code{future} (default: detectCores()-1).}

\item{blas_threads}{BLAS/OMP threads (default 1).}

\item{use_biocparallel}{Logical; if TRUE, also register a BiocParallel backend.}
}
\value{
Invisibly returns a list with chosen backends.
}
\description{
Uses only \code{future} by default. Optionally registers a BiocParallel backend
with correct types (\code{type="SOCK"}). Keeps BLAS/OMP to 1 thread to avoid oversubscription.
}
\examples{
set_parallel_profile_v2("auto", workers = 16, use_biocparallel = FALSE)
}
