% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_working.R
\name{LDS}
\alias{LDS}
\title{Limma-Dream-SVA (LDS) 파이프라인}
\usage{
LDS(
  sobj,
  formula,
  meta.data = NULL,
  layer = "counts",
  n_sv = NULL,
  sv_var_cutoff = 0.5,
  n_cores = max(1, parallel::detectCores() - 2)
)
}
\arguments{
\item{sobj}{Seurat 객체, DGEList 객체, 또는 Raw Count Matrix.}

\item{formula}{(formula) 모델 포뮬러. LMM 수식(lme4)을 따릅니다.
(예: ~ response + (1|emrid) + (1|set))}

\item{meta.data}{(data.frame) 메타데이터. \code{sobj}가 Matrix일 경우 필수.}

\item{layer}{(character) Seurat 객체 사용 시, Raw Count가 저장된 layer.}

\item{n_sv}{(numeric) 사용할 SV의 개수. \code{NULL} (기본값)일 경우,
\code{sv_var_cutoff}에 따라 자동으로 결정됩니다.}

\item{sv_var_cutoff}{(numeric) \code{n_sv=NULL}일 때, SV가 설명해야 할
*잔차 분산(residual variance)*의 누적 비율 (0 ~ 1).}

\item{n_cores}{(numeric) 병렬 처리에 사용할 CPU 코어 수.}
}
\value{
(list) 'fit' (MArrayLM 객체), 'voom' (EList 객체),
'sva' (SVA 객체), 'final_formula' (사용된 최종 포뮬러)
}
\description{
SVA로 숨겨진 공변량을 찾고, limma-dream으로 다중 임의 효과
(예: 환자, 배치)를 포함한 LMM을 피팅합니다. 기본적으로 GeoMx data와 같이 샘플 수가 많지 않은 경우에 사용됩니다.
}
