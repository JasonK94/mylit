% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe_demulti.R, R/utils_demulti.R
\name{generate_sample_names}
\alias{generate_sample_names}
\title{Generate Sample Names}
\usage{
generate_sample_names(
  n_samples = NULL,
  format = "Sample \%d",
  include_doublets = TRUE,
  n_total_cols = NULL,
  start_num = 1,
  vector = NULL
)

generate_sample_names(
  n_samples = NULL,
  format = "Sample \%d",
  include_doublets = TRUE,
  n_total_cols = NULL,
  start_num = 1,
  vector = NULL
)
}
\arguments{
\item{n_samples}{Number of samples (if NULL, will be inferred from n_total_cols or vector)}

\item{format}{Format string with \\%d placeholder (default: "Sample \\%d")}

\item{include_doublets}{Whether to include doublet combinations (default: TRUE)}

\item{n_total_cols}{Total number of columns (singlets + doublets). If provided, n_samples will be calculated.}

\item{start_num}{Starting number for sample names (default: 1)}

\item{vector}{Vector of sample numbers/names (for backward compatibility with old code)}
}
\value{
Character vector of sample names

Character vector of formatted sample names
}
\description{
Generates sample names in the format used by the pipeline

Creates formatted sample name lists for display/plotting.
Supports both old-style (vector input) and new-style (n_samples/n_total_cols) usage.
}
\examples{
generate_sample_names(3, format = "Patient \%d")
# Returns: c("Patient 1", "Patient 2", "Patient 3", 
#            "Patient 1+Patient 2", "Patient 1+Patient 3", "Patient 2+Patient 3")

# Infer n_samples from total columns (e.g., from demux file)
generate_sample_names(n_total_cols = 21)  # 6 singlets + 15 doublets = 21
# Returns: c("Sample 1", "Sample 2", ..., "Sample 6", "Sample 1+Sample 2", ...)

# Old-style usage (backward compatibility)
generate_sample_names(vector = 1:6)
# Returns: c("1", "2", ..., "6", "1+2", "1+3", ...)

}
