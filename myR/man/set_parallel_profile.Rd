% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_gpt.R
\name{set_parallel_profile}
\alias{set_parallel_profile}
\title{Set parallel & threading profile for RStudio vs Terminal}
\usage{
set_parallel_profile(
  mode = c("auto", "rstudio", "terminal"),
  workers = max(1, parallel::detectCores() - 1),
  blas_threads = 1
)
}
\arguments{
\item{mode}{One of c("rstudio","terminal","auto").
\itemize{
\item "rstudio": use PSOCK / multisession (no forking).
\item "terminal": use Multicore where possible.
\item "auto": detect RStudio and choose automatically.
}}

\item{workers}{Integer number of workers. Default: parallel::detectCores()-1.}

\item{blas_threads}{Integer BLAS threads (via RhpcBLASctl if available).}
}
\value{
Invisibly returns a list with chosen backends.
}
\description{
Set parallel & threading profile for RStudio vs Terminal
}
\examples{
set_parallel_profile(mode="auto", workers=8, blas_threads=1)
}
