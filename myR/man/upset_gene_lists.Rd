% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{upset_gene_lists}
\alias{upset_gene_lists}
\title{UpSet Plot for Multiple Gene Lists}
\usage{
upset_gene_lists(
  gene_lists,
  sets = NULL,
  min_size = 1,
  width_ratio = 0.3,
  keep_sets = NULL
)
}
\arguments{
\item{gene_lists}{Named list. 각 요소가 character vector인 gene list.}

\item{sets}{Optional character vector. 표시할 세트 순서를 names(gene_lists)가 아닌 다른 순서로 지정할 때 사용.
기본은 names(gene_lists) 순서.}

\item{min_size}{Integer. 최소 교집합 크기(유전자 수)가 이 값 이상인 경우에만 표시 (default: 1).}

\item{width_ratio}{Numeric. 메인 플롯과 바 플롯 너비 비율 (default: 0.3).}

\item{keep_sets}{Character vector. 특정 세트만 플롯에 포함시키고 싶을 때 사용. 기본 NULL(모두 포함).}
}
\value{
ggplot 객체.
}
\description{
주어진 여러 유전자 벡터(list)를 이용해 UpSet 플롯을 그립니다.
}
\examples{
\dontrun{
L <- list(
  A = c("TP53","EGFR","MYC","BRCA1"),
  B = c("EGFR","MYC","PTEN","BRCA2"),
  C = c("MYC","PTEN","ALK")
)
p <- upset_gene_lists(L)
print(p)
}
}
