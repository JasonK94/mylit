% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test.R
\name{train_meta_learner_v1}
\alias{train_meta_learner_v1}
\title{Train a Meta-Learner (L2 Stacking Model)}
\usage{
train_meta_learner_v1(
  l1_signatures,
  holdout_data,
  target_var,
  l2_methods = c("glm", "ranger", "xgbTree"),
  k_folds = 5,
  metric = "AUC",
  fgs_seed = 42
)
}
\arguments{
\item{l1_signatures}{A named list of trained L1 signatures (outputs from
                     find_gene_signature). e.g., list(lasso=sig1, rf=sig2)}

\item{holdout_data}{A Seurat object or matrix (genes x cells) NOT used
                     for training L1 models.}

\item{target_var}{The target variable column name in holdout_data@meta.data.}

\item{l2_methods}{A vector of model methods supported by caret
                  (e.g., c("glm", "ranger", "xgbTree", "svmRadial")).}

\item{k_folds}{Number of folds for cross-validation to select the best L2 model.}

\item{metric}{The metric to optimize (e.g., "AUC", "Accuracy").
              (Note: For "AUC", target must be binary factor).}

\item{fgs_seed}{Seed for reproducibility.}
}
\value{
A list containing:
   - $best_model: The final L2 model (a caret 'train' object)
                    trained on all l2_train_df.
   - $model_comparison: Results from caret::resamples comparing L2 candidates.
   - $l2_train_df: The dataframe used for L2 training (L1 scores + target).
   - $l1_signatures: The provided L1 signatures (for reference).
}
\description{
Trains multiple L2 candidate models on L1 scores and selects the best
one based on cross-validation performance.
}
