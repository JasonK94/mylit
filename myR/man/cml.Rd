% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{cml}
\alias{cml}
\title{Cumulative Line Graph of Cluster Proportions}
\usage{
cml(
  sobj,
  cluster_col = "seurat_clusters",
  group.by = "gem",
  sort.by = c("name", "frequency"),
  df = FALSE,
  color_palette = NULL,
  n_patterns = 5,
  n_shapes = 7
)
}
\arguments{
\item{sobj}{A \strong{Seurat} object containing the study metadata.}

\item{cluster_col}{Metadata column with cluster IDs (default
\code{"seurat_clusters"}).}

\item{group.by}{Metadata column identifying samples or other groups
to compare (default \code{"gem"}).}

\item{sort.by}{Cluster ordering strategy: \code{"name"} (default)
keeps the natural/alphabetical order, whereas
\code{"frequency"} orders clusters by their global
frequency (descending).}

\item{df}{Logical; return underlying summary data instead of
the plot (default \code{FALSE}).}

\item{color_palette}{Optional vector of colours for groups. If \code{NULL}
a Set2 palette is generated.}

\item{n_patterns, n_shapes}{How many distinct linetypes / shapes to cycle.}
}
\value{
A \code{ggplot} object (or \code{data.frame} when \code{df = TRUE}).
}
\description{
Produces a cumulative line plot of per‑cluster cell proportions for each
sample (or other grouping variable). Clusters can be ordered alphabetically
/numerically by name, or by their overall frequency in the dataset.
}
