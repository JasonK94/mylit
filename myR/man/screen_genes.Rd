% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_claude.R
\name{screen_genes}
\alias{screen_genes}
\title{Quick Gene Screening Using Wilcoxon Tests}
\usage{
screen_genes(
  seurat_obj,
  config,
  grouping_var = "timepoint",
  subset_var = NULL,
  subset_value = NULL,
  min_pct = 0.1,
  logfc_threshold = 0.1,
  top_n = 1000,
  test_method = "wilcox"
)
}
\arguments{
\item{seurat_obj}{Seurat object.}

\item{config}{List. Configuration from create_analysis_config().}

\item{grouping_var}{String. Which variable to compare ("timepoint", "drug",
"response", or name of a derived column like "condition_full").}

\item{subset_var}{String. Optional. Metadata column to subset by.}

\item{subset_value}{Value to filter subset_var by (e.g., "Tumor" for tissue type).}

\item{min_pct}{Numeric. Minimum fraction of samples expressing gene in either group.}

\item{logfc_threshold}{Numeric. Minimum log fold-change threshold.}

\item{top_n}{Integer. Number of top genes to return.}

\item{test_method}{String. Statistical test method (default "wilcox").}
}
\value{
A list containing:
\describe{
\item{all_markers}{Full results data frame from FindAllMarkers}
\item{top_genes}{Character vector of top N gene names}
\item{subset_obj}{The Seurat object used (potentially subsetted)}
}
}
\description{
Performs rapid statistical screening to identify candidate genes for
computationally intensive LMM analysis. Uses Seurat's FindAllMarkers
for efficiency.
}
\examples{
# Screen for genes changing between timepoints
screening <- screen_genes(
  seurat_obj = sobj,
  config = config,
  grouping_var = "timepoint",
  top_n = 500
)

# Use top genes for LMM analysis
lmm_results <- run_lmm_analysis(
  seurat_obj = sobj,
  genes = screening$top_genes,
  config = config
)
}
