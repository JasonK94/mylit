% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{scatter_smooth_colored}
\alias{scatter_smooth_colored}
\title{Averaged Expression vs Numeric Covariate (with optional colour)}
\usage{
scatter_smooth_colored(
  object,
  feature,
  group.by = "sample_no",
  x_var = "numeric_value",
  transpose = FALSE,
  color_by = NULL,
  palette = NULL,
  transparency = TRUE,
  transparency_desc = FALSE,
  fitted_line = c("linear", "loess", "lasso", NULL)
)
}
\arguments{
\item{object}{A \strong{Seurat} object \emph{or} a data.frame. When data.frame is
supplied it must already contain the columns listed below.}

\item{feature}{Character; gene symbol (Seurat) or expression column name
(data.frame).}

\item{group.by}{Column used to aggregate cells into samples (default
"sample_no").}

\item{x_var}{Column providing the numeric predictor \emph{per cell} (will be
averaged just like expression). Typical clinical scores.}

\item{transpose}{Logical; if TRUE swap X and Y axes (default FALSE).}

\item{color_by}{NULL (no colour) or a column name to colour points.}

\item{palette}{Optional palette vector / name passed to scale_*; if NULL
defaults are used.}

\item{transparency}{Logical; map \code{color_by} (numeric only) to point
alpha (default TRUE).}

\item{transparency_desc}{Logical; when TRUE, higher values become more
transparent (inverse mapping).}

\item{fitted_line}{One of "linear" (default), "loess", "lasso", or NULL.}
}
\value{
A \code{ggplot} scatter‑smooth.
}
\description{
For each sample (\code{group.by}), the function first averages gene
expression (\code{feature}) across cells, then scatters these averages
against a numeric covariate (\code{x_var}). Points can be coloured by a
categorical or numeric column, transparency scaled, and different smoothing
methods added.
}
