% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lds_corrplot.R
\name{lds_corrplot}
\alias{lds_corrplot}
\title{SVA와 메타데이터 간 상관관계 플롯 생성}
\usage{
lds_corrplot(
  sva,
  meta.data,
  method = c("spearman", "pearson"),
  display.value = FALSE,
  clustering = TRUE,
  top_n = NULL,
  min_corr = 0.1,
  output_file = NULL,
  width = 12,
  height = 8,
  ...
)
}
\arguments{
\item{sva}{SVA 객체 또는 SV 매트릭스 (행=셀, 열=SV)}

\item{meta.data}{메타데이터 (행=셀, 열=변수)}

\item{method}{상관관계 계산 방법 ("spearman" 또는 "pearson"). 기본값 "spearman"}

\item{display.value}{상관관계 값을 플롯에 표시할지 여부. 기본값 FALSE}

\item{clustering}{계층적 클러스터링 적용 여부. 기본값 TRUE}

\item{top_n}{상관관계가 높은 상위 n개 변수만 표시 (NULL이면 모두 표시). 기본값 NULL}

\item{min_corr}{최소 상관관계 절댓값 (이보다 작은 값은 필터링). 기본값 0.1}

\item{output_file}{출력 파일 경로 (NULL이면 플롯만 반환). 기본값 NULL}

\item{width}{플롯 너비 (인치). 기본값 12}

\item{height}{플롯 높이 (인치). 기본값 8}

\item{...}{추가 파라미터 (pheatmap 또는 corrplot에 전달)}
}
\value{
상관관계 행렬 (invisible)
}
\description{
SVA 객체(또는 SV 매트릭스)와 메타데이터를 받아 상관관계 플롯 생성
}
