% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudobulk_deg.R
\name{FindMarkers_pseudobulk}
\alias{FindMarkers_pseudobulk}
\title{Advanced Pseudobulk FindMarkers function with covariates}
\usage{
FindMarkers_pseudobulk(
  seurat_obj,
  ident.1,
  ident.2 = NULL,
  group.by = "seurat_clusters",
  sample.by,
  covariates = NULL,
  aggregate.by = NULL,
  min.cells = 10,
  method = "DESeq2",
  design.formula = NULL,
  contrast.type = "simple",
  logfc.threshold = 0.25,
  min.pct = 0.1,
  assay = DefaultAssay(seurat_obj),
  slot = "counts"
)

FindMarkers_pseudobulk(
  seurat_obj,
  ident.1,
  ident.2 = NULL,
  group.by = "seurat_clusters",
  sample.by,
  covariates = NULL,
  aggregate.by = NULL,
  min.cells = 10,
  method = "DESeq2",
  design.formula = NULL,
  contrast.type = "simple",
  logfc.threshold = 0.25,
  min.pct = 0.1,
  assay = DefaultAssay(seurat_obj),
  slot = "counts"
)
}
\arguments{
\item{seurat_obj}{Seurat object}

\item{ident.1}{Identity class to define markers for}

\item{ident.2}{Identity class(es) to compare against (default: all others)}

\item{group.by}{Column name in metadata to use for identity classes}

\item{sample.by}{Column name in metadata defining biological replicates/samples}

\item{covariates}{Character vector of additional metadata columns to include in the model}

\item{aggregate.by}{Additional column to create finer pseudobulk aggregations (e.g., subcluster)}

\item{min.cells}{Minimum number of cells per pseudobulk sample}

\item{method}{DE method to use: "DESeq2" or "edgeR"}

\item{design.formula}{Custom design formula (overrides automatic formula generation)}

\item{contrast.type}{Type of contrast: "simple" or "interaction"}

\item{logfc.threshold}{Log fold change threshold}

\item{min.pct}{Minimum percentage of samples expressing the gene}

\item{assay}{Assay to use}

\item{slot}{Slot to use (counts recommended for pseudobulk)}
}
\value{
Data frame with DE results

Data frame with DE results
}
\description{
Advanced Pseudobulk FindMarkers function with covariates

Advanced Pseudobulk FindMarkers function with covariates
}
