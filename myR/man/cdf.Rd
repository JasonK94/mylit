% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{cdf}
\alias{cdf}
\title{Compute and Plot Cumulative Distribution Function (CDF)}
\usage{
cdf(
  data,
  probability_col,
  ratio_col,
  plot_type = "probability",
  output_file = NULL
)
}
\arguments{
\item{data}{\code{data.frame} 형태의 입력 데이터.}

\item{probability_col}{확률 값을 가진 컬럼명. 문자열(\code{"Best_Probability"}) 또는
bare name(\code{Best_Probability}) 형태로 지정 가능.}

\item{ratio_col}{확률 비율 값을 가진 컬럼명. 문자열 또는 bare name 형태 지정.}

\item{plot_type}{CDF를 계산할 변수 유형.
\itemize{
\item \code{"probability"}: 원본 확률 값
\item \code{"logit"}: 확률의 로그잇 변환 (\code{log(p/(1-p))})
\item \code{"ratio"}: 확률 비율 (로그 스케일)
기본값은 \code{"probability"}.
}}

\item{output_file}{(선택) 결과 플롯을 저장할 파일 경로(예: \code{"cdf_plot.png"}).
지정하지 않으면 화면에만 출력.}
}
\value{
\code{list}
\describe{
\item{plot}{ggplot2 객체, CDF 그래프}
\item{summary}{\code{data.frame}, 변수별 요약 통계(Mean, Median, SD, Min, Max)}
}
}
\description{
이 함수는 주어진 데이터 프레임에서 확률, 로그잇(logit) 변환된 확률,
또는 비율에 대해 누적분포함수(CDF)를 계산하고, ggplot2를 이용해
CDF 그래프를 그립니다. 필요시 파일로도 저장할 수 있으며, 요약 통계량도 반환합니다.
}
\details{
\itemize{
\item \code{probability_col}, \code{ratio_col} 에는 \code{data} 내 존재하는 컬럼명을 지정해야 합니다.
\item \code{plot_type="ratio"} 인 경우 x축이 로그 스케일로 표현됩니다.
\item 반환값으로는 \code{list(plot, summary)} 형태를 가지며,
\itemize{
\item \code{plot}: ggplot2 객체
\item \code{summary}: 평균, 중앙값, 표준편차, 최소값, 최대값을 포함한 요약 통계 데이터프레임
}
}
}
\examples{
\dontrun{
# bare name 사용 예
result <- cdf(df, Best_Probability, Probability_Ratio, plot_type = "logit")
print(result$plot)

# 문자열 사용 예
result2 <- cdf(df, "Best_Probability", "Probability_Ratio", plot_type = "ratio",
               output_file = "my_cdf.png")
head(result2$summary)
}

}
