% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_validation.R
\name{validate_analysis_inputs}
\alias{validate_analysis_inputs}
\title{분석 입력값 검증 헬퍼 함수}
\usage{
validate_analysis_inputs(
  data,
  metadata = NULL,
  target_vars = NULL,
  control_vars = NULL,
  formula = NULL,
  method = NULL,
  auto_correct_vars = FALSE,
  auto_correct_formula = FALSE,
  na_action = c("report", "remove_rows")
)
}
\arguments{
\item{data}{기본 데이터 객체 (data.frame, matrix, Seurat 등).}

\item{metadata}{메타데이터 (data.frame). NULL일 경우 \code{data}에서 추출을 시도합니다.}

\item{target_vars}{분석 대상 변수 (캐릭터 벡터).}

\item{control_vars}{통제 변수 (캐릭터 벡터).}

\item{formula}{분석에 사용될 포뮬러 (formula 또는 character).}

\item{method}{분석 방법론 (character, 예: "lm", "wilcoxon", "random forest").}

\item{auto_correct_vars}{(logical) TRUE일 경우, 문자형(character) 변수를 팩터(factor)로 자동 변환합니다.}

\item{auto_correct_formula}{(logical) TRUE일 경우, \code{method}에 맞게 포뮬러 수정을 시도합니다 (예: ML에 랜덤 이펙트 제거).}

\item{na_action}{(character) 'report'(기본값)는 NA를 보고만 하고, 'remove_rows'는 \code{target_vars}, \code{control_vars}에
지정된 변수들에 NA가 있는 행을 제거한 \code{metadata_clean}을 반환합니다.}
}
\value{
검증이 완료되고 (필요시) 수정된 \code{metadata}, \code{formula} 및 (na_action='remove_rows'인 경우)
NA가 제거된 \code{metadata_clean}을 포함하는 리스트(list).
}
\description{
통계 분석/모델링에 사용될 데이터, 메타데이터, 변수, 포뮬러, 방법론을 검증합니다.
}
\examples{
\dontrun{
# ... (이전 예제 코드와 동일) ...

# 3. NA 제거 옵션 테스트
validated_na_removed <- validate_analysis_inputs(
  data = sim_data,
  target_vars = "target",
  control_vars = c("group", "age", "sex"), # 'age'에 NA가 있음
  formula = "target ~ group + age + sex",
  method = "lm",
  na_action = "remove_rows"
)

# 원본 메타데이터 행 개수
# nrow(validated_na_removed$metadata) 
# NA 제거된 메타데이터 행 개수
# nrow(validated_na_removed$metadata_clean)
}
}
